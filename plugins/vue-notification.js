export default defineNuxtPlugin((nuxtApp) => {
  // This plugin is disabled for now since vue-notifications is not compatible with Vue 3/Nuxt 3
  // Consider using vue-toast-notification which is already in dependencies
  // or @nuxtjs/toast which is also available
})
